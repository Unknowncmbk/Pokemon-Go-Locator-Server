CREATE TABLE IF NOT EXISTS `pokemon` (
`id` INT NOT NULL, 
`name` VARCHAR(30) NOT NULL, 
PRIMARY KEY (`id`)
) ENGINE = InnoDB;

CREATE TABLE IF NOT EXISTS `pokemon_radar` (
`pokemon_id` INT NOT NULL,
`creation` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, 
`expiration` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, 
`lat` FLOAT( 10, 6 ) NOT NULL,
`lng` FLOAT( 10, 6 ) NOT NULL, 
PRIMARY KEY(`lat`, `lng`, `pokemon_id`), 
FOREIGN KEY (pokemon_id) REFERENCES pokemon(id) ON DELETE CASCADE
) ENGINE = InnoDB;

CREATE TABLE IF NOT EXISTS `pokemon_request` (
`id` INT NOT NULL AUTO_INCREMENT PRIMARY KEY, 
`lat` FLOAT( 10, 6 ) NOT NULL, 
`lng` FLOAT( 10, 6 ) NOT NULL, 
`creation` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE = InnoDB;

SELECT UNIX_TIMESTAMP(expiration) - UNIX_TIMESTAMP() as time_left FROM pokemon_radar;
162.243.166.250:5000/req/40.860836/-82.32043799999997